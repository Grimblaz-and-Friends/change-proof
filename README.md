# change-proof

A reusable GitHub workflow that proves a pull request before it merges: its executable checks ran, a use was bought where the changed paths require one, and every connected reviewer ran once and had each comment answered. The workflow and its contract land by pull request.
